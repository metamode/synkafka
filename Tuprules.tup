CFLAGS += -Wall -std=c++11

ifeq (@(TUP_PLATFORM),macosx)
	CFLAGS += -stdlib=libc++
endif 

#CXX = gccfilter -c -a g++
CXX = g++

ifdef DBG
	CFLAGS += -ggdb
else
	CFLAGS += -O3
endif

!cc = |> $(CXX) $(CFLAGS) -c %f -o %o |> %B.o