include_rules

CFLAGS += -I ../vendor/snappy -I ../vendor/spdlog/include -pthread

: foreach *.cpp |> !cc |>
: foreach ../vendor/snappy/snappy.cc ../vendor/snappy/snappy-sinksource.cc ../vendor/snappy/snappy-stubs-internal.cc |> !cc |>
: *.o |> ar rcs %o %f |> libsynkafka.a